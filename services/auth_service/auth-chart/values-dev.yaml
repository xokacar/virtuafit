replicaCount: 1

app:
  name: auth-service
  namespace: vf-dev
  environment: dev

image:
  repository: auth_service  
  tag: "latest"
  pullPolicy: IfNotPresent   

env:
  - name: DATABASE_URL
    valueFrom:
      secretKeyRef:
        name: auth-service-psql-secret
        key: DATABASE_URL        

service:
  type: ClusterIP
  port: 8080
  targetPort: 5000

probes:
  path: "/health"
  initialDelaySeconds: 10

imagePullSecrets: []